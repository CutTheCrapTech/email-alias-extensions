{
  "name": "@email-alias-extensions/chrome",
  "version": "0.1.0",
  "private": true,
  "main": "dist/popup.js",
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "watch": "tsc --build --watch",
    "test": "cross-env NODE_OPTIONS=--experimental-vm-modules jest"
  }
}

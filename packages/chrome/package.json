{
  "name": "@email-alias-extensions/chrome",
  "version": "0.1.0",
  "private": true,
  "main": "dist/popup.js",
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "watch": "tsc --build --watch",
    "test": "jest"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "jsdom",
    "globals": {
      "ts-jest": {
        "useESM": true
      }
    }
  }
}
